const { describe, it } = require("mocha");
const { expect } = require("chai");
const BalanceMenuOption = require("@menu-root/balance-option");

describe("Balance Menu Option", () => {
  it("creates account if it absent", () => {
    expect(1).to.be.equal(1);
  });
});
